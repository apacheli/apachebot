# apachebot

### About

development bot for whirlybird

### Running

- Set an environment variable for `$BOT_TOKEN`
- Run `deno task start` to start the bot

### Development

Run `deno fmt` and `deno lint` for code styling.
